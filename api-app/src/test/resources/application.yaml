spring:
  profiles:
    active: test
  application:
    name: api-app
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
  security:
    user:
      name: user
      password: password
      roles:
        - USER
